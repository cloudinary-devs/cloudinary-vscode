/**
 * Preview panel functionality.
 * Just initializes common functionality (tabs, lightbox, copy buttons).
 */

import { initCommon } from "./common";

// Initialize when script loads
initCommon();
